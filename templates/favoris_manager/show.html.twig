{% extends 'base.html.twig' %}

{% block title %}Favoris{% endblock %}

{% block body %}
    <h1>Favoris</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_favoris_manager_index') }}">back to list</a>

    <a href="{{ path('app_favoris_manager_edit', {'id': favori.id}) }}">edit</a>

    {{ include('favoris_manager/_delete_form.html.twig') }}
{% endblock %}
