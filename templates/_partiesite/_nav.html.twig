
<header>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand navsaf" href="{{ path('app_accueil') }}">SAFER</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_accueil') }}">Home</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('app_categorie_index') }}">Categorie</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('app_bien_manage_index') }}">Bien</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('app_favoris_manager_index') }}">Favoris</a>
          </li>
        {% endif %}
        {% if is_granted('ROLE_USER') %}
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/bien/profil">Bien</a>
          </li>  
        {% endif %}   
        <li class="nav-item">
          <a class="nav-link" href="#">Contactez nous</a>
        </li>
      </ul>

      {# Pour l'affichage de personne connecté#}
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Me connecter</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">M'inscrire</a>
          </li>
        {% endif %}
      </ul>

    </div>
  </div>
</nav>
<header>